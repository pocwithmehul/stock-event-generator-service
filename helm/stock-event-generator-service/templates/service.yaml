apiVersion: v1
kind: Service
metadata:
  name: {{ include "stock-event-generator-service.fullname" . }}
  labels:
    {{- include "stock-event-generator-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "stock-event-generator-service.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
